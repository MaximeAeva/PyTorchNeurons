name: pytorch_env
channels:
      - conda-forge
      - nvidia
      - pytorch
dependencies:
      - python>=3.9
      - nvidia::cudatoolkit=11.5
      - pytorch::pytorch
      - numpy
      - matplotlib
      - pandas
      - pillow
      - pip
